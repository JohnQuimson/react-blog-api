import React, { useState } from 'react';
const apiUrl = import.meta.env.VITE_BASE_API_URL;

const ElencoArticles = () => {
  const [articles, setArticles] = useState(null);

  return <>ElencoArticles</>;
};

export default ElencoArticles;
